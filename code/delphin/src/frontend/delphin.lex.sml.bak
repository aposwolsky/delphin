functor DelphinLexFun(structure Tokens : Delphin_TOKENS
                               structure Interface : INTERFACE)  = struct

    structure yyInput : sig

        type stream
	val mkStream : (int -> string) -> stream
	val fromStream : TextIO.StreamIO.instream -> stream
	val getc : stream -> (Char.char * stream) option
	val getpos : stream -> int
	val getlineNo : stream -> int
	val subtract : stream * stream -> string
	val eof : stream -> bool
	val lastWasNL : stream -> bool

      end = struct

        structure TIO = TextIO
        structure TSIO = TIO.StreamIO
	structure TPIO = TextPrimIO

        datatype stream = Stream of {
            strm : TSIO.instream,
	    id : int,  (* track which streams originated 
			* from the same stream *)
	    pos : int,
	    lineNo : int,
	    lastWasNL : bool
          }

	local
	  val next = ref 0
	in
	fun nextId() = !next before (next := !next + 1)
	end

	val initPos = 2 (* ml-lex bug compatibility *)

	fun mkStream inputN = let
              val strm = TSIO.mkInstream 
			   (TPIO.RD {
			        name = "lexgen",
				chunkSize = 4096,
				readVec = SOME inputN,
				readArr = NONE,
				readVecNB = NONE,
				readArrNB = NONE,
				block = NONE,
				canInput = NONE,
				avail = (fn () => NONE),
				getPos = NONE,
				setPos = NONE,
				endPos = NONE,
				verifyPos = NONE,
				close = (fn () => ()),
				ioDesc = NONE
			      }, "")
	      in 
		Stream {strm = strm, id = nextId(), pos = initPos, lineNo = 1,
			lastWasNL = true}
	      end

	fun fromStream strm = Stream {
		strm = strm, id = nextId(), pos = initPos, lineNo = 1, lastWasNL = true
	      }

	fun getc (Stream {strm, pos, id, lineNo, ...}) = (case TSIO.input1 strm
              of NONE => NONE
	       | SOME (c, strm') => 
		   SOME (c, Stream {
			        strm = strm', 
				pos = pos+1, 
				id = id,
				lineNo = lineNo + 
					 (if c = #"\n" then 1 else 0),
				lastWasNL = (c = #"\n")
			      })
	     (* end case*))

	fun getpos (Stream {pos, ...}) = pos

	fun getlineNo (Stream {lineNo, ...}) = lineNo

	fun subtract (new, old) = let
	      val Stream {strm = strm, pos = oldPos, id = oldId, ...} = old
	      val Stream {pos = newPos, id = newId, ...} = new
              val (diff, _) = if newId = oldId andalso newPos >= oldPos
			      then TSIO.inputN (strm, newPos - oldPos)
			      else raise Fail 
				"BUG: yyInput: attempted to subtract incompatible streams"
	      in 
		diff 
	      end

	fun eof (Stream {strm, ...}) = TSIO.endOfStream strm

	fun lastWasNL (Stream {lastWasNL, ...}) = lastWasNL

      end

    datatype yystart_state = 
LF | COMMENT | INITIAL
    structure UserDeclarations = 
      struct

(* Lexer for Delphin *)
(* Author:  Adam Poswolsky *)

structure Tokens = Tokens
structure Interface = Interface

type pos = Interface.pos

type svalue = Tokens.svalue
type ('a, 'b) token = ('a, 'b) Tokens.token
type lexresult = (svalue, pos) token

val numOpeningComments = ref 0 
val toPos = Interface.intToPos

(* The twelf printer for Regions will add 1 since it counts from 0
 * so this is to just adjust 
 *)
fun toReg(a,b) = Paths.Reg(a-1,b-1)

(*
 * This function is called when we hit EOF
 *)
  fun eof () = 
    let 
        val pos = Interface.noPos
    in 
    (
     if (!numOpeningComments > 0) then
        Interface.error (("unclosed comment at eof."), pos, pos)
      else ()
      ;
      numOpeningComments := 0;
     (* If we reset, then the parser errors are messed up, it
        must be reset before not after.
      Interface.reset();
       *)
     Tokens.EOF(pos,pos))
    end

  (* Below, we will not allow the following characters (besides whitespace) in uiden
   , ; : {  } (  )  [  ]  <  > # @ *
   and the following strings cannot be used as uiden
   _ * , -> <- = | . => 
     and type fun unit case of pop let in end val fn eps
  *)



      end

    datatype yymatch 
      = yyNO_MATCH
      | yyMATCH of yyInput.stream * action * yymatch
    withtype action = yyInput.stream * yymatch -> UserDeclarations.lexresult

    local

    val yytable = 
#[
]

    fun mk yyins = let
        (* current start state *)
        val yyss = ref INITIAL
	fun YYBEGIN ss = (yyss := ss)
	(* current input stream *)
        val yystrm = ref yyins
	(* get one char of input *)
	val yygetc = yyInput.getc
	(* create yytext *)
	fun yymktext(strm) = yyInput.subtract (strm, !yystrm)
        open UserDeclarations
        fun lex 
(yyarg as ()) = let 
     fun continue() = let
            val yylastwasn = yyInput.lastWasNL (!yystrm)
            fun yystuck (yyNO_MATCH) = raise Fail "stuck state"
	      | yystuck (yyMATCH (strm, action, old)) = 
		  action (strm, old)
	    val yypos = yyInput.getpos (!yystrm)
	    val yygetlineNo = yyInput.getlineNo
	    fun yyactsToMatches (strm, [],	  oldMatches) = oldMatches
	      | yyactsToMatches (strm, act::acts, oldMatches) = 
		  yyMATCH (strm, act, yyactsToMatches (strm, acts, oldMatches))
	    fun yygo actTable = 
		(fn (~1, _, oldMatches) => yystuck oldMatches
		  | (curState, strm, oldMatches) => let
		      val (transitions, finals') = Vector.sub (yytable, curState)
		      val finals = map (fn i => Vector.sub (actTable, i)) finals'
		      fun tryfinal() = 
		            yystuck (yyactsToMatches (strm, finals, oldMatches))
		      fun find (c, []) = NONE
			| find (c, (c1, c2, s)::ts) = 
		            if c1 <= c andalso c <= c2 then SOME s
			    else find (c, ts)
		      in case yygetc strm
			  of SOME(c, strm') => 
			       (case find (c, transitions)
				 of NONE => tryfinal()
				  | SOME n => 
				      yygo actTable
					(n, strm', 
					 yyactsToMatches (strm, finals, oldMatches)))
			   | NONE => tryfinal()
		      end)
	    in 
let
fun yyAction0 (strm, lastMatch : yymatch) = (yystrm := strm; (continue()))
fun yyAction1 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Paths.newLine(yypos) ; Interface.incLineNum(yypos); continue()))
fun yyAction2 (strm, lastMatch : yymatch) = let
      val yytext = yymktext(strm)
      in
        yystrm := strm;
        (Tokens.USE(substring(yytext, 4, size(yytext)-4), 
                                  toPos yypos, toPos(yypos + size(yytext)-1)))
      end
fun yyAction3 (strm, lastMatch : yymatch) = (yystrm := strm;
      (numOpeningComments := 1 ; 
                                    YYBEGIN COMMENT; 
                                    continue()))
fun yyAction4 (strm, lastMatch : yymatch) = (yystrm := strm;
      (numOpeningComments := !numOpeningComments + 1;
                                    continue()))
fun yyAction5 (strm, lastMatch : yymatch) = (yystrm := strm;
      (
                                    numOpeningComments := !numOpeningComments - 1;
                                    if (!numOpeningComments = 0) then 
                                      ( YYBEGIN INITIAL ; continue() )
                                    else
                                      continue()
                                   ))
fun yyAction6 (strm, lastMatch : yymatch) = (yystrm := strm; (continue()))
fun yyAction7 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.ABBREV(toReg(yypos,yypos+6), toPos yypos, toPos (yypos+6))))
fun yyAction8 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.INFIX(toReg(yypos,yypos+5), toPos yypos, toPos (yypos+5))))
fun yyAction9 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.POSTFIX(toReg(yypos,yypos+7), toPos yypos, toPos (yypos+7))))
fun yyAction10 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.PREFIX(toReg(yypos,yypos+6), toPos yypos, toPos (yypos+6))))
fun yyAction11 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.NAME(toReg(yypos,yypos+4), toPos yypos, toPos (yypos+4))))
fun yyAction12 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.RIGHT(toReg(yypos,yypos+5), toPos yypos, toPos (yypos+4))))
fun yyAction13 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.LEFT(toReg(yypos,yypos+4), toPos yypos, toPos (yypos+3))))
fun yyAction14 (strm, lastMatch : yymatch) = let
      val yytext = yymktext(strm)
      in
        yystrm := strm;
        (Tokens.DIGITS((toReg(yypos, yypos + size(yytext)-1), valOf (Int.fromString(yytext))),
                                     toPos yypos,toPos (yypos + size(yytext)-1)))
      end
fun yyAction15 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.UNDERSCORE(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction16 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.STAR(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction17 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.COMMA(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction18 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.POUND(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction19 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.DOT(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction20 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.RTARROW(toReg(yypos, yypos + 1), toPos yypos, toPos(yypos+1))))
fun yyAction21 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.EQARROW(toReg(yypos, yypos + 1), toPos yypos, toPos(yypos+1))))
fun yyAction22 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.LTARROW(toReg(yypos, yypos + 1), toPos yypos, toPos(yypos+1))))
fun yyAction23 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.EQUAL(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction24 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.BAR(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction25 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.BACKSLASH(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction26 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.AND(toReg(yypos, yypos+2), toPos yypos,toPos (yypos+2))))
fun yyAction27 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.TYPE(toReg(yypos, yypos + 3), toPos yypos, toPos(yypos +3))))
fun yyAction28 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.FUN(toReg(yypos, yypos + 2), toPos yypos,toPos (yypos+2))))
fun yyAction29 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.UNIT(toReg(yypos, yypos + 3), toPos yypos, toPos (yypos+3))))
fun yyAction30 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.CASE(toReg(yypos, yypos + 3), toPos yypos,toPos (yypos+3))))
fun yyAction31 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.SIG(toReg(yypos, yypos + 2), toPos yypos,toPos (yypos+2))))
fun yyAction32 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.OF(toReg(yypos, yypos + 1), toPos yypos,toPos (yypos+1))))
fun yyAction33 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.POP(toReg(yypos, yypos + 2), toPos yypos,toPos (yypos +2))))
fun yyAction34 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.END(toReg(yypos, yypos + 2), toPos yypos,toPos (yypos +2))))
fun yyAction35 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.VAL(toReg(yypos, yypos + 2), toPos yypos,toPos (yypos +2))))
fun yyAction36 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.LET(toReg(yypos, yypos + 2), toPos yypos,toPos (yypos+2))))
fun yyAction37 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.IN(toReg(yypos, yypos + 1), toPos yypos,toPos (yypos+1))))
fun yyAction38 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.FN(toReg(yypos, yypos + 1), toPos yypos,toPos (yypos+1))))
fun yyAction39 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.SEMICOLON(toReg(yypos, yypos), toPos yypos, toPos(yypos ))))
fun yyAction40 (strm, lastMatch : yymatch) = (yystrm := strm;
      (YYBEGIN LF;
				    Tokens.DBL_LTANGLE(toReg(yypos, yypos+1), toPos yypos,toPos (yypos+1))))
fun yyAction41 (strm, lastMatch : yymatch) = (yystrm := strm;
      (YYBEGIN LF;
				    Tokens.LTANGLE(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction42 (strm, lastMatch : yymatch) = (yystrm := strm;
      (YYBEGIN INITIAL ;
				    Tokens.DBL_RTANGLE(toReg(yypos, yypos+1), toPos yypos,toPos (yypos+1))))
fun yyAction43 (strm, lastMatch : yymatch) = (yystrm := strm;
      (YYBEGIN INITIAL ;
				    Tokens.RTANGLE(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction44 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.LTPAREN(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction45 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.RTPAREN(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction46 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.LTBRACKET(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction47 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.RTBRACKET(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction48 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.LTBRACE(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction49 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.RTBRACE(toReg(yypos, yypos), toPos yypos,toPos (yypos))))
fun yyAction50 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.COLON(toReg(yypos, yypos), toPos yypos,toPos(yypos))))
fun yyAction51 (strm, lastMatch : yymatch) = (yystrm := strm;
      (Tokens.AT(toReg(yypos, yypos), toPos yypos,toPos(yypos))))
fun yyAction52 (strm, lastMatch : yymatch) = let
      val yytext = yymktext(strm)
      in
        yystrm := strm;
        (Tokens.ID((toReg(yypos, yypos + size(yytext)-1), yytext),toPos yypos,toPos (yypos+size(yytext)-1)))
      end
fun yyAction53 (strm, lastMatch : yymatch) = let
      val yytext = yymktext(strm)
      in
        yystrm := strm;
        (Tokens.ID((toReg(yypos, yypos + size(yytext)-1), yytext),toPos yypos,toPos (yypos+size(yytext)-1)))
      end
fun yyAction54 (strm, lastMatch : yymatch) = let
      val yytext = yymktext(strm)
      in
        yystrm := strm;
        (Interface.error ("ignoring bad character " ^ yytext,
                                    toPos yypos, toPos (yypos)); continue())
      end
fun yyQ20 (strm, lastMatch : yymatch) = yyAction49(strm, yyNO_MATCH)
fun yyQ64 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ63 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction24(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction24(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction24(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction24(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                      else yyAction24(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction24(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction24(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                  else yyAction24(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction24(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                      else yyAction24(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction24(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction24(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction24, yyNO_MATCH))
                  else yyAction24(strm, yyNO_MATCH)
              else yyAction24(strm, yyNO_MATCH)
      (* end case *))
fun yyQ19 (strm, lastMatch : yymatch) = yyAction48(strm, yyNO_MATCH)
fun yyQ66 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction35(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction35(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction35(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction35(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                      else yyAction35(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction35(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction35(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                  else yyAction35(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction35(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                      else yyAction35(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction35(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction35(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction35, yyNO_MATCH))
                  else yyAction35(strm, yyNO_MATCH)
              else yyAction35(strm, yyNO_MATCH)
      (* end case *))
fun yyQ65 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"m"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"m"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"l"
                  then yyQ66(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ62 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"b"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"b"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"a"
                  then yyQ65(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ73 (strm, lastMatch : yymatch) = yyAction2(strm, yyNO_MATCH)
fun yyQ72 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yystuck(lastMatch)
        | SOME(inp, strm') =>
            if inp = #"\""
              then yyQ73(strm', lastMatch)
            else if inp < #"\""
              then if inp = #"!"
                  then yyQ72(strm', lastMatch)
                  else yystuck(lastMatch)
            else if inp <= #"~"
              then yyQ72(strm', lastMatch)
              else yystuck(lastMatch)
      (* end case *))
fun yyQ71 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yystuck(lastMatch)
        | SOME(inp, strm') =>
            if inp = #"\""
              then yystuck(lastMatch)
            else if inp < #"\""
              then if inp = #"!"
                  then yyQ72(strm', lastMatch)
                  else yystuck(lastMatch)
            else if inp <= #"~"
              then yyQ72(strm', lastMatch)
              else yystuck(lastMatch)
      (* end case *))
fun yyQ70 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yystuck(lastMatch)
        | SOME(inp, strm') =>
            if inp = #"!"
              then yystuck(lastMatch)
            else if inp < #"!"
              then if inp = #" "
                  then yyQ70(strm', lastMatch)
                  else yystuck(lastMatch)
            else if inp = #"\""
              then yyQ71(strm', lastMatch)
              else yystuck(lastMatch)
      (* end case *))
fun yyQ69 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"("
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"("
                  then if inp = #"!"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"!"
                      then if inp = #" "
                          then yyQ70(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                          else yyAction52(strm, yyNO_MATCH)
                    else if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"-"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"-"
                  then if inp = #"+"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ68 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"f"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"e"
                  then yyQ69(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ75 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction29(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction29(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction29(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction29(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                      else yyAction29(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction29(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction29(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                  else yyAction29(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction29(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                      else yyAction29(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction29(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction29(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction29, yyNO_MATCH))
                  else yyAction29(strm, yyNO_MATCH)
              else yyAction29(strm, yyNO_MATCH)
      (* end case *))
fun yyQ74 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"u"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"u"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"t"
                  then yyQ75(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ67 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"j"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"j"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"i"
                  then yyQ74(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ61 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"@"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"@"
              then if inp = #","
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #","
                  then if inp = #"$"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                        else if inp < #"!"
                          then yyAction52(strm, yyNO_MATCH)
                        else if inp = #"#"
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"("
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"+"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"="
                  then if inp <= #"9"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #">"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"t"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"t"
              then if inp = #"n"
                  then yyQ67(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"n"
                  then if inp = #"["
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"["
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"]"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"s"
                  then yyQ68(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ77 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction31(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction31(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction31(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction31(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                      else yyAction31(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction31(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction31(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                  else yyAction31(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction31(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                      else yyAction31(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction31(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction31(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction31, yyNO_MATCH))
                  else yyAction31(strm, yyNO_MATCH)
              else yyAction31(strm, yyNO_MATCH)
      (* end case *))
fun yyQ76 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"h"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"h"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"g"
                  then yyQ77(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ60 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"j"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"j"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"i"
                  then yyQ76(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ81 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction12(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction12(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction12(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction12(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                      else yyAction12(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction12(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction12(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                  else yyAction12(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction12(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                      else yyAction12(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction12(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction12(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction12, yyNO_MATCH))
                  else yyAction12(strm, yyNO_MATCH)
              else yyAction12(strm, yyNO_MATCH)
      (* end case *))
fun yyQ80 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"u"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"u"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"t"
                  then yyQ81(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ79 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"i"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"i"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"h"
                  then yyQ80(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ78 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"h"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"h"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"g"
                  then yyQ79(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ59 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"j"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"j"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"i"
                  then yyQ78(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ83 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction33(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction33(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction33(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction33(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                      else yyAction33(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction33(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction33(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                  else yyAction33(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction33(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                      else yyAction33(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction33(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction33(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction33, yyNO_MATCH))
                  else yyAction33(strm, yyNO_MATCH)
              else yyAction33(strm, yyNO_MATCH)
      (* end case *))
fun yyQ82 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"q"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"q"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"p"
                  then yyQ83(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ58 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"p"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"p"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"o"
                  then yyQ82(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ84 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction32(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction32(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction32(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction32(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                      else yyAction32(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction32(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction32(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                  else yyAction32(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction32(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                      else yyAction32(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction32(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction32(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction32, yyNO_MATCH))
                  else yyAction32(strm, yyNO_MATCH)
              else yyAction32(strm, yyNO_MATCH)
      (* end case *))
fun yyQ57 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"g"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"g"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"f"
                  then yyQ84(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ87 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction36(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction36(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction36(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction36(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                      else yyAction36(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction36(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction36(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                  else yyAction36(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction36(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                      else yyAction36(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction36(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction36(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction36, yyNO_MATCH))
                  else yyAction36(strm, yyNO_MATCH)
              else yyAction36(strm, yyNO_MATCH)
      (* end case *))
fun yyQ88 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction13(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction13(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction13(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction13(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                      else yyAction13(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction13(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction13(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                  else yyAction13(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction13(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                      else yyAction13(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction13(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction13(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction13, yyNO_MATCH))
                  else yyAction13(strm, yyNO_MATCH)
              else yyAction13(strm, yyNO_MATCH)
      (* end case *))
fun yyQ86 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"u"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"u"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"t"
                  then yyQ88(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ85 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"@"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"@"
              then if inp = #","
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #","
                  then if inp = #"$"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                        else if inp < #"!"
                          then yyAction52(strm, yyNO_MATCH)
                        else if inp = #"#"
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"("
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"+"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"="
                  then if inp <= #"9"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #">"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"u"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"u"
              then if inp = #"f"
                  then yyQ86(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"f"
                  then if inp = #"["
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"["
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"]"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"t"
                  then yyQ87(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ56 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"f"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"e"
                  then yyQ85(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ89 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction37(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction37(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction37(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction37(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                      else yyAction37(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction37(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction37(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                  else yyAction37(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction37(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                      else yyAction37(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction37(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction37(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction37, yyNO_MATCH))
                  else yyAction37(strm, yyNO_MATCH)
              else yyAction37(strm, yyNO_MATCH)
      (* end case *))
fun yyQ55 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"o"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"o"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"n"
                  then yyQ89(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ92 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction28(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction28(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction28(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction28(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                      else yyAction28(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction28(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction28(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                  else yyAction28(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction28(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                      else yyAction28(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction28(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction28(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction28, yyNO_MATCH))
                  else yyAction28(strm, yyNO_MATCH)
              else yyAction28(strm, yyNO_MATCH)
      (* end case *))
fun yyQ91 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"o"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"o"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"n"
                  then yyQ92(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ90 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction38(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction38(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction38(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction38(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                      else yyAction38(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction38(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction38(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                  else yyAction38(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction38(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                      else yyAction38(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction38(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction38(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction38, yyNO_MATCH))
                  else yyAction38(strm, yyNO_MATCH)
              else yyAction38(strm, yyNO_MATCH)
      (* end case *))
fun yyQ54 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"@"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"@"
              then if inp = #","
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #","
                  then if inp = #"$"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                        else if inp < #"!"
                          then yyAction52(strm, yyNO_MATCH)
                        else if inp = #"#"
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"("
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"+"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"="
                  then if inp <= #"9"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #">"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"v"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"v"
              then if inp = #"n"
                  then yyQ90(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"n"
                  then if inp = #"["
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"["
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"]"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"u"
                  then yyQ91(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ94 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction34(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction34(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction34(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction34(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                      else yyAction34(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction34(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction34(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                  else yyAction34(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction34(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                      else yyAction34(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction34(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction34(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction34, yyNO_MATCH))
                  else yyAction34(strm, yyNO_MATCH)
              else yyAction34(strm, yyNO_MATCH)
      (* end case *))
fun yyQ93 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"e"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"e"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"d"
                  then yyQ94(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ53 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"o"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"o"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"n"
                  then yyQ93(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ97 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction30(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction30(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction30(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction30(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                      else yyAction30(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction30(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction30(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                  else yyAction30(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction30(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                      else yyAction30(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction30(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction30(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction30, yyNO_MATCH))
                  else yyAction30(strm, yyNO_MATCH)
              else yyAction30(strm, yyNO_MATCH)
      (* end case *))
fun yyQ96 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"f"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"e"
                  then yyQ97(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ95 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"t"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"t"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"s"
                  then yyQ96(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ52 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"b"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"b"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"a"
                  then yyQ95(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ99 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction26(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction26(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction26(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction26(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                      else yyAction26(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction26(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction26(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                  else yyAction26(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction26(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                      else yyAction26(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction26(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction26(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction26, yyNO_MATCH))
                  else yyAction26(strm, yyNO_MATCH)
              else yyAction26(strm, yyNO_MATCH)
      (* end case *))
fun yyQ98 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"e"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"e"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"d"
                  then yyQ99(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ51 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"o"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"o"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"n"
                  then yyQ98(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ50 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction15(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction15(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction15(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction15(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                      else yyAction15(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction15(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction15(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                  else yyAction15(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction15(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                      else yyAction15(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction15(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction15(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                  else yyAction15(strm, yyNO_MATCH)
              else yyAction15(strm, yyNO_MATCH)
      (* end case *))
fun yyQ16 (strm, lastMatch : yymatch) = yyAction47(strm, yyNO_MATCH)
fun yyQ49 (strm, lastMatch : yymatch) = yyAction25(strm, yyNO_MATCH)
fun yyQ15 (strm, lastMatch : yymatch) = yyAction46(strm, yyNO_MATCH)
fun yyQ48 (strm, lastMatch : yymatch) = yyAction51(strm, yyNO_MATCH)
fun yyQ100 (strm, lastMatch : yymatch) = yyAction21(strm, yyNO_MATCH)
fun yyQ47 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction23(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyQ100(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction23(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction23(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                      else yyAction23(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction23(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction23(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                  else yyAction23(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction23(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                      else yyAction23(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction23(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction23(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                  else yyAction23(strm, yyNO_MATCH)
              else yyAction23(strm, yyNO_MATCH)
      (* end case *))
fun yyQ102 (strm, lastMatch : yymatch) = yyAction40(strm, yyNO_MATCH)
fun yyQ101 (strm, lastMatch : yymatch) = yyAction22(strm, yyNO_MATCH)
fun yyQ46 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction41(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"."
              then yyAction41(strm, yyNO_MATCH)
            else if inp < #"."
              then if inp = #"-"
                  then yyQ101(strm', yyMATCH(strm, yyAction41, yyNO_MATCH))
                  else yyAction41(strm, yyNO_MATCH)
            else if inp = #"<"
              then yyQ102(strm', yyMATCH(strm, yyAction41, yyNO_MATCH))
              else yyAction41(strm, yyNO_MATCH)
      (* end case *))
fun yyQ45 (strm, lastMatch : yymatch) = yyAction39(strm, yyNO_MATCH)
fun yyQ11 (strm, lastMatch : yymatch) = yyAction50(strm, yyNO_MATCH)
fun yyQ103 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction14(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction14(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction14(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction14(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                      else yyAction14(strm, yyNO_MATCH)
                else if inp = #"0"
                  then yyQ103(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp < #"0"
                  then if inp = #","
                      then yyAction14(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp = #":"
                  then yyAction14(strm, yyNO_MATCH)
                else if inp < #":"
                  then yyQ103(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                  else yyAction14(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction14(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                      else yyAction14(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction14(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction14(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                  else yyAction14(strm, yyNO_MATCH)
              else yyAction14(strm, yyNO_MATCH)
      (* end case *))
fun yyQ44 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction14(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction14(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction14(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction14(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                      else yyAction14(strm, yyNO_MATCH)
                else if inp = #"0"
                  then yyQ103(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp < #"0"
                  then if inp = #","
                      then yyAction14(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp = #":"
                  then yyAction14(strm, yyNO_MATCH)
                else if inp < #":"
                  then yyQ103(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                  else yyAction14(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction14(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                      else yyAction14(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction14(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction14(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction14, yyNO_MATCH))
                  else yyAction14(strm, yyNO_MATCH)
              else yyAction14(strm, yyNO_MATCH)
      (* end case *))
fun yyQ43 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction19(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction19(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction19(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction19(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                      else yyAction19(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction19(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction19(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                  else yyAction19(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction19(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                      else yyAction19(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction19(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction19(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction19, yyNO_MATCH))
                  else yyAction19(strm, yyNO_MATCH)
              else yyAction19(strm, yyNO_MATCH)
      (* end case *))
fun yyQ104 (strm, lastMatch : yymatch) = yyAction20(strm, yyNO_MATCH)
fun yyQ42 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyQ104(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ41 (strm, lastMatch : yymatch) = yyAction17(strm, yyNO_MATCH)
fun yyQ40 (strm, lastMatch : yymatch) = yyAction16(strm, yyNO_MATCH)
fun yyQ9 (strm, lastMatch : yymatch) = yyAction45(strm, yyNO_MATCH)
fun yyQ105 (strm, lastMatch : yymatch) = yyAction3(strm, yyNO_MATCH)
fun yyQ39 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction44(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"*"
              then yyQ105(strm', yyMATCH(strm, yyAction44, yyNO_MATCH))
              else yyAction44(strm, yyNO_MATCH)
      (* end case *))
fun yyQ115 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction10(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction10(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction10(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction10(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                      else yyAction10(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction10(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction10(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                  else yyAction10(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction10(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                      else yyAction10(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction10(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction10(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction10, yyNO_MATCH))
                  else yyAction10(strm, yyNO_MATCH)
              else yyAction10(strm, yyNO_MATCH)
      (* end case *))
fun yyQ114 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"y"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"y"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"x"
                  then yyQ115(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ113 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"j"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"j"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"i"
                  then yyQ114(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ112 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"g"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"g"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"f"
                  then yyQ113(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ111 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"f"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"e"
                  then yyQ112(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ120 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction9(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction9(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction9(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction9(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                      else yyAction9(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction9(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction9(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                  else yyAction9(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction9(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                      else yyAction9(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction9(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction9(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction9, yyNO_MATCH))
                  else yyAction9(strm, yyNO_MATCH)
              else yyAction9(strm, yyNO_MATCH)
      (* end case *))
fun yyQ119 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"y"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"y"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"x"
                  then yyQ120(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ118 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"j"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"j"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"i"
                  then yyQ119(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ117 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"g"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"g"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"f"
                  then yyQ118(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ116 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"u"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"u"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"t"
                  then yyQ117(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ110 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"t"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"t"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"s"
                  then yyQ116(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ109 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"@"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"@"
              then if inp = #","
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #","
                  then if inp = #"$"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                        else if inp < #"!"
                          then yyAction52(strm, yyNO_MATCH)
                        else if inp = #"#"
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"("
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"+"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"="
                  then if inp <= #"9"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #">"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"s"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"s"
              then if inp = #"o"
                  then yyQ110(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"o"
                  then if inp = #"["
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"["
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"]"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"r"
                  then yyQ111(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ123 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction11(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction11(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction11(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction11(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                      else yyAction11(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction11(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction11(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                  else yyAction11(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction11(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                      else yyAction11(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction11(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction11(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction11, yyNO_MATCH))
                  else yyAction11(strm, yyNO_MATCH)
              else yyAction11(strm, yyNO_MATCH)
      (* end case *))
fun yyQ122 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"f"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"e"
                  then yyQ123(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ121 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"n"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"n"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"m"
                  then yyQ122(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ108 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"b"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"b"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"a"
                  then yyQ121(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ127 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction8(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction8(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction8(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction8(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                      else yyAction8(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction8(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction8(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                  else yyAction8(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction8(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                      else yyAction8(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction8(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction8(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction8, yyNO_MATCH))
                  else yyAction8(strm, yyNO_MATCH)
              else yyAction8(strm, yyNO_MATCH)
      (* end case *))
fun yyQ126 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"y"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"y"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"x"
                  then yyQ127(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ125 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"j"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"j"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"i"
                  then yyQ126(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ124 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"g"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"g"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"f"
                  then yyQ125(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ107 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"o"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"o"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"n"
                  then yyQ124(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ132 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction7(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction7(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction7(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction7(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                      else yyAction7(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction7(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction7(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                  else yyAction7(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction7(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                      else yyAction7(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction7(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction7(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction7, yyNO_MATCH))
                  else yyAction7(strm, yyNO_MATCH)
              else yyAction7(strm, yyNO_MATCH)
      (* end case *))
fun yyQ131 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"w"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"w"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"v"
                  then yyQ132(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ130 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"f"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"e"
                  then yyQ131(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ129 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"s"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"s"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"r"
                  then yyQ130(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ128 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"c"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"c"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"b"
                  then yyQ129(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ106 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"?"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"?"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"c"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"c"
              then if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #"@"
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"^"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"^"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp = #"b"
                  then yyQ128(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"}"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ38 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"["
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"["
              then if inp = #"-"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"-"
                  then if inp = #"$"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                        else if inp < #"!"
                          then yyAction52(strm, yyNO_MATCH)
                        else if inp = #"#"
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"+"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"+"
                      then if inp <= #"'"
                          then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                          else yyAction52(strm, yyNO_MATCH)
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #">"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #">"
                  then if inp = #":"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #":"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp = #"="
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"@"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"o"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"o"
              then if inp = #"b"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"b"
                  then if inp = #"^"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp < #"^"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp = #"a"
                      then yyQ106(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"j"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"j"
                  then if inp = #"i"
                      then yyQ107(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"n"
                  then yyQ108(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"|"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"|"
              then if inp = #"q"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"q"
                  then yyQ109(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"{"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ7 (strm, lastMatch : yymatch) = yyAction18(strm, yyNO_MATCH)
fun yyQ37 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction52(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"+"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"+"
                  then if inp = #"#"
                      then yyAction52(strm, yyNO_MATCH)
                    else if inp < #"#"
                      then if inp <= #" "
                          then yyAction52(strm, yyNO_MATCH)
                          else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                    else if inp <= #"'"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #":"
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #","
                      then yyAction52(strm, yyNO_MATCH)
                      else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp = #"="
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
            else if inp = #"{"
              then yyAction52(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"A"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                      else yyAction52(strm, yyNO_MATCH)
                else if inp = #"["
                  then yyAction52(strm, yyNO_MATCH)
                else if inp < #"["
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                else if inp <= #"]"
                  then yyAction52(strm, yyNO_MATCH)
                  else yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp = #"~"
              then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ64(strm', yyMATCH(strm, yyAction52, yyNO_MATCH))
                  else yyAction52(strm, yyNO_MATCH)
              else yyAction52(strm, yyNO_MATCH)
      (* end case *))
fun yyQ5 (strm, lastMatch : yymatch) = yyAction1(strm, yyNO_MATCH)
fun yyQ30 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction0(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"\n"
              then yyAction0(strm, yyNO_MATCH)
            else if inp < #"\n"
              then if inp = #"\t"
                  then yyQ30(strm', yyMATCH(strm, yyAction0, yyNO_MATCH))
                  else yyAction0(strm, yyNO_MATCH)
            else if inp = #" "
              then yyQ30(strm', yyMATCH(strm, yyAction0, yyNO_MATCH))
              else yyAction0(strm, yyNO_MATCH)
      (* end case *))
fun yyQ4 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction0(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"\n"
              then yyAction0(strm, yyNO_MATCH)
            else if inp < #"\n"
              then if inp = #"\t"
                  then yyQ30(strm', yyMATCH(strm, yyAction0, yyNO_MATCH))
                  else yyAction0(strm, yyNO_MATCH)
            else if inp = #" "
              then yyQ30(strm', yyMATCH(strm, yyAction0, yyNO_MATCH))
              else yyAction0(strm, yyNO_MATCH)
      (* end case *))
fun yyQ3 (strm, lastMatch : yymatch) = yyAction54(strm, yyNO_MATCH)
fun yyQ2 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE =>
            if yyInput.eof(!(yystrm))
              then UserDeclarations.eof(yyarg)
              else yystuck(lastMatch)
        | SOME(inp, strm') =>
            if inp = #"]"
              then yyQ16(strm', lastMatch)
            else if inp < #"]"
              then if inp = #","
                  then yyQ41(strm', lastMatch)
                else if inp < #","
                  then if inp = #"$"
                      then yyQ37(strm', lastMatch)
                    else if inp < #"$"
                      then if inp = #"\v"
                          then yyQ3(strm', lastMatch)
                        else if inp < #"\v"
                          then if inp = #"\t"
                              then yyQ4(strm', lastMatch)
                            else if inp = #"\n"
                              then yyQ5(strm', lastMatch)
                              else yyQ3(strm', lastMatch)
                        else if inp = #"!"
                          then yyQ37(strm', lastMatch)
                        else if inp < #"!"
                          then if inp = #" "
                              then yyQ4(strm', lastMatch)
                              else yyQ3(strm', lastMatch)
                        else if inp = #"#"
                          then yyQ7(strm', lastMatch)
                          else yyQ37(strm', lastMatch)
                    else if inp = #")"
                      then yyQ9(strm', lastMatch)
                    else if inp < #")"
                      then if inp = #"&"
                          then yyQ37(strm', lastMatch)
                        else if inp < #"&"
                          then yyQ38(strm', lastMatch)
                        else if inp = #"("
                          then yyQ39(strm', lastMatch)
                          else yyQ37(strm', lastMatch)
                    else if inp = #"*"
                      then yyQ40(strm', lastMatch)
                      else yyQ37(strm', lastMatch)
                else if inp = #"="
                  then yyQ47(strm', lastMatch)
                else if inp < #"="
                  then if inp = #"0"
                      then yyQ44(strm', lastMatch)
                    else if inp < #"0"
                      then if inp = #"."
                          then yyQ43(strm', lastMatch)
                        else if inp = #"-"
                          then yyQ42(strm', lastMatch)
                          else yyQ37(strm', lastMatch)
                    else if inp = #";"
                      then yyQ45(strm', lastMatch)
                    else if inp < #";"
                      then if inp = #":"
                          then yyQ11(strm', lastMatch)
                          else yyQ44(strm', lastMatch)
                      else yyQ46(strm', lastMatch)
                else if inp = #"A"
                  then yyQ37(strm', lastMatch)
                else if inp < #"A"
                  then if inp = #"?"
                      then yyQ37(strm', lastMatch)
                    else if inp = #">"
                      then yyQ3(strm', lastMatch)
                      else yyQ48(strm', lastMatch)
                else if inp = #"["
                  then yyQ15(strm', lastMatch)
                else if inp = #"\\"
                  then yyQ49(strm', lastMatch)
                  else yyQ37(strm', lastMatch)
            else if inp = #"o"
              then yyQ57(strm', lastMatch)
            else if inp < #"o"
              then if inp = #"e"
                  then yyQ53(strm', lastMatch)
                else if inp < #"e"
                  then if inp = #"a"
                      then yyQ51(strm', lastMatch)
                    else if inp < #"a"
                      then if inp = #"_"
                          then yyQ50(strm', lastMatch)
                          else yyQ37(strm', lastMatch)
                    else if inp = #"c"
                      then yyQ52(strm', lastMatch)
                      else yyQ37(strm', lastMatch)
                else if inp = #"j"
                  then yyQ37(strm', lastMatch)
                else if inp < #"j"
                  then if inp = #"g"
                      then yyQ37(strm', lastMatch)
                    else if inp < #"g"
                      then yyQ54(strm', lastMatch)
                    else if inp = #"i"
                      then yyQ55(strm', lastMatch)
                      else yyQ37(strm', lastMatch)
                else if inp = #"l"
                  then yyQ56(strm', lastMatch)
                  else yyQ37(strm', lastMatch)
            else if inp = #"v"
              then yyQ62(strm', lastMatch)
            else if inp < #"v"
              then if inp = #"s"
                  then yyQ60(strm', lastMatch)
                else if inp < #"s"
                  then if inp = #"q"
                      then yyQ37(strm', lastMatch)
                    else if inp = #"p"
                      then yyQ58(strm', lastMatch)
                      else yyQ59(strm', lastMatch)
                else if inp = #"t"
                  then yyQ37(strm', lastMatch)
                  else yyQ61(strm', lastMatch)
            else if inp = #"}"
              then yyQ20(strm', lastMatch)
            else if inp < #"}"
              then if inp = #"{"
                  then yyQ19(strm', lastMatch)
                else if inp = #"|"
                  then yyQ63(strm', lastMatch)
                  else yyQ37(strm', lastMatch)
            else if inp = #"~"
              then yyQ37(strm', lastMatch)
              else yyQ3(strm', lastMatch)
      (* end case *))
fun yyQ35 (strm, lastMatch : yymatch) = yyAction5(strm, yyNO_MATCH)
fun yyQ34 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction6(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #")"
              then yyQ35(strm', yyMATCH(strm, yyAction6, yyNO_MATCH))
              else yyAction6(strm, yyNO_MATCH)
      (* end case *))
fun yyQ36 (strm, lastMatch : yymatch) = yyAction4(strm, yyNO_MATCH)
fun yyQ33 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction6(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"*"
              then yyQ36(strm', yyMATCH(strm, yyAction6, yyNO_MATCH))
              else yyAction6(strm, yyNO_MATCH)
      (* end case *))
fun yyQ32 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction0(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"\n"
              then yyAction0(strm, yyNO_MATCH)
            else if inp < #"\n"
              then if inp = #"\t"
                  then yyQ30(strm', yyMATCH(strm, yyAction0, yyNO_MATCH))
                  else yyAction0(strm, yyNO_MATCH)
            else if inp = #" "
              then yyQ30(strm', yyMATCH(strm, yyAction0, yyNO_MATCH))
              else yyAction0(strm, yyNO_MATCH)
      (* end case *))
fun yyQ31 (strm, lastMatch : yymatch) = yyAction6(strm, yyNO_MATCH)
fun yyQ1 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE =>
            if yyInput.eof(!(yystrm))
              then UserDeclarations.eof(yyarg)
              else yystuck(lastMatch)
        | SOME(inp, strm') =>
            if inp = #"!"
              then yyQ31(strm', lastMatch)
            else if inp < #"!"
              then if inp = #"\n"
                  then yyQ5(strm', lastMatch)
                else if inp < #"\n"
                  then if inp = #"\t"
                      then yyQ32(strm', lastMatch)
                      else yyQ31(strm', lastMatch)
                else if inp = #" "
                  then yyQ32(strm', lastMatch)
                  else yyQ31(strm', lastMatch)
            else if inp = #")"
              then yyQ31(strm', lastMatch)
            else if inp < #")"
              then if inp = #"("
                  then yyQ33(strm', lastMatch)
                  else yyQ31(strm', lastMatch)
            else if inp = #"*"
              then yyQ34(strm', lastMatch)
              else yyQ31(strm', lastMatch)
      (* end case *))
fun yyQ23 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyAction53(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"{"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"\\"
                  then if inp = #"["
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"]"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyAction53(strm, yyNO_MATCH)
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
and yyQ22 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyAction53(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyAction53(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ21 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyAction53(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"{"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"\\"
                  then if inp = #"["
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"]"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyAction53(strm, yyNO_MATCH)
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ26 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction27(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction27(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                          else yyAction27(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction27(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                      else yyAction27(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction27(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                else if inp = #":"
                  then yyAction27(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction27(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyAction27(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
                  else yyAction27(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction27(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction27(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction27, yyNO_MATCH))
              else yyAction27(strm, yyNO_MATCH)
      (* end case *))
fun yyQ25 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"-"
                  then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"-"
                  then if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"("
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp <= #")"
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"="
                  then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"f"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"f"
              then if inp = #"]"
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"]"
                  then if inp = #"["
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"e"
                  then yyQ26(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ24 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"-"
                  then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"-"
                  then if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"("
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp <= #")"
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"="
                  then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"q"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"q"
              then if inp = #"]"
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"]"
                  then if inp = #"["
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"p"
                  then yyQ25(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ18 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #">"
              then if inp = #"-"
                  then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"-"
                  then if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"("
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"("
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp <= #")"
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #":"
                  then if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"="
                  then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"z"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"z"
              then if inp = #"]"
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"]"
                  then if inp = #"["
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"y"
                  then yyQ24(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ17 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction15(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction15(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                          else yyAction15(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction15(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                      else yyAction15(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction15(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp = #":"
                  then yyAction15(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction15(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyAction15(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
                  else yyAction15(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction15(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction15(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction15, yyNO_MATCH))
              else yyAction15(strm, yyNO_MATCH)
      (* end case *))
fun yyQ27 (strm, lastMatch : yymatch) = yyAction42(strm, yyNO_MATCH)
fun yyQ14 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction43(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #">"
              then yyQ27(strm', yyMATCH(strm, yyAction43, yyNO_MATCH))
              else yyAction43(strm, yyNO_MATCH)
      (* end case *))
fun yyQ13 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction23(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction23(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                          else yyAction23(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction23(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                      else yyAction23(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction23(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp = #":"
                  then yyAction23(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp = #"{"
              then yyAction23(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp < #"\\"
                  then if inp = #"["
                      then yyAction23(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                else if inp = #"]"
                  then yyAction23(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ22(strm', yyMATCH(strm, yyAction23, yyNO_MATCH))
                  else yyAction23(strm, yyNO_MATCH)
              else yyAction23(strm, yyNO_MATCH)
      (* end case *))
fun yyQ28 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction22(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction22(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                          else yyAction22(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction22(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                      else yyAction22(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction22(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                else if inp = #":"
                  then yyAction22(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
            else if inp = #"{"
              then yyAction22(strm, yyNO_MATCH)
            else if inp < #"{"
              then if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                else if inp < #"\\"
                  then if inp = #"["
                      then yyAction22(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                else if inp = #"]"
                  then yyAction22(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
            else if inp < #"~"
              then if inp = #"|"
                  then yyQ22(strm', yyMATCH(strm, yyAction22, yyNO_MATCH))
                  else yyAction22(strm, yyNO_MATCH)
              else yyAction22(strm, yyNO_MATCH)
      (* end case *))
fun yyQ12 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyAction53(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ28(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyAction53(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ29 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction20(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction20(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                          else yyAction20(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction20(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                      else yyAction20(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction20(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                else if inp = #":"
                  then yyAction20(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction20(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyAction20(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
                  else yyAction20(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction20(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction20(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction20, yyNO_MATCH))
              else yyAction20(strm, yyNO_MATCH)
      (* end case *))
fun yyQ10 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyAction53(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyQ29(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyAction53(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ8 (strm, lastMatch : yymatch) = yyAction44(strm, yyNO_MATCH)
fun yyQ6 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE => yyAction53(strm, yyNO_MATCH)
        | SOME(inp, strm') =>
            if inp = #"="
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"="
              then if inp = #"*"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"*"
                  then if inp = #"\""
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp < #"\""
                      then if inp = #"!"
                          then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                          else yyAction53(strm, yyNO_MATCH)
                    else if inp = #"$"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp < #"$"
                      then yyAction53(strm, yyNO_MATCH)
                    else if inp <= #"'"
                      then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                      else yyAction53(strm, yyNO_MATCH)
                else if inp = #"/"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp < #"/"
                  then if inp = #"-"
                      then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                    else if inp = #"."
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #":"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"^"
              then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp < #"^"
              then if inp = #"["
                  then yyAction53(strm, yyNO_MATCH)
                else if inp < #"["
                  then if inp = #">"
                      then yyAction53(strm, yyNO_MATCH)
                      else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                else if inp = #"\\"
                  then yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
                  else yyAction53(strm, yyNO_MATCH)
            else if inp = #"}"
              then yyAction53(strm, yyNO_MATCH)
            else if inp < #"}"
              then if inp = #"{"
                  then yyAction53(strm, yyNO_MATCH)
                  else yyQ22(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
            else if inp = #"~"
              then yyQ23(strm', yyMATCH(strm, yyAction53, yyNO_MATCH))
              else yyAction53(strm, yyNO_MATCH)
      (* end case *))
fun yyQ0 (strm, lastMatch : yymatch) = (case (yygetc(strm))
       of NONE =>
            if yyInput.eof(!(yystrm))
              then UserDeclarations.eof(yyarg)
              else yystuck(lastMatch)
        | SOME(inp, strm') =>
            if inp = #"<"
              then yyQ12(strm', lastMatch)
            else if inp < #"<"
              then if inp = #"$"
                  then yyQ6(strm', lastMatch)
                else if inp < #"$"
                  then if inp = #" "
                      then yyQ4(strm', lastMatch)
                    else if inp < #" "
                      then if inp = #"\n"
                          then yyQ5(strm', lastMatch)
                        else if inp < #"\n"
                          then if inp = #"\t"
                              then yyQ4(strm', lastMatch)
                              else yyQ3(strm', lastMatch)
                          else yyQ3(strm', lastMatch)
                    else if inp = #"\""
                      then yyQ3(strm', lastMatch)
                    else if inp = #"!"
                      then yyQ6(strm', lastMatch)
                      else yyQ7(strm', lastMatch)
                else if inp = #"-"
                  then yyQ10(strm', lastMatch)
                else if inp < #"-"
                  then if inp = #")"
                      then yyQ9(strm', lastMatch)
                    else if inp < #")"
                      then if inp = #"("
                          then yyQ8(strm', lastMatch)
                          else yyQ6(strm', lastMatch)
                      else yyQ6(strm', lastMatch)
                else if inp = #":"
                  then yyQ11(strm', lastMatch)
                else if inp < #":"
                  then if inp = #"."
                      then yyQ3(strm', lastMatch)
                      else yyQ6(strm', lastMatch)
                  else yyQ6(strm', lastMatch)
            else if inp = #"`"
              then yyQ6(strm', lastMatch)
            else if inp < #"`"
              then if inp = #"\\"
                  then yyQ6(strm', lastMatch)
                else if inp < #"\\"
                  then if inp = #"?"
                      then yyQ6(strm', lastMatch)
                    else if inp < #"?"
                      then if inp = #"="
                          then yyQ13(strm', lastMatch)
                          else yyQ14(strm', lastMatch)
                    else if inp = #"["
                      then yyQ15(strm', lastMatch)
                      else yyQ6(strm', lastMatch)
                else if inp = #"^"
                  then yyQ6(strm', lastMatch)
                else if inp = #"]"
                  then yyQ16(strm', lastMatch)
                  else yyQ17(strm', lastMatch)
            else if inp = #"|"
              then yyQ6(strm', lastMatch)
            else if inp < #"|"
              then if inp = #"u"
                  then yyQ6(strm', lastMatch)
                else if inp < #"u"
                  then if inp = #"t"
                      then yyQ18(strm', lastMatch)
                      else yyQ6(strm', lastMatch)
                else if inp = #"{"
                  then yyQ19(strm', lastMatch)
                  else yyQ6(strm', lastMatch)
            else if inp = #"~"
              then yyQ21(strm', lastMatch)
            else if inp = #"}"
              then yyQ20(strm', lastMatch)
              else yyQ3(strm', lastMatch)
      (* end case *))
in
  (case (!(yyss))
   of LF => yyQ0(!(yystrm), yyNO_MATCH)
    | COMMENT => yyQ1(!(yystrm), yyNO_MATCH)
    | INITIAL => yyQ2(!(yystrm), yyNO_MATCH)
  (* end case *))
end
            end
	  in 
            continue() 	  
	    handle IO.Io{cause, ...} => raise cause
          end
        in 
          lex 
        end
    in
    fun makeLexer yyinputN = mk (yyInput.mkStream yyinputN)
    fun makeLexer' ins = mk (yyInput.mkStream ins)
    end

  end
